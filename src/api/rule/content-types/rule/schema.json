{
  "kind": "collectionType",
  "collectionName": "rules",
  "info": {
    "singularName": "rule",
    "pluralName": "rules",
    "displayName": "Rule"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "rule_type": {
      "type": "enumeration",
      "enum": [
        "capacity",
        "progress",
        "cost"
      ]
    },
    "severity": {
      "type": "enumeration",
      "enum": [
        "info",
        "warning",
        "critical"
      ]
    },
    "operation": {
      "type": "enumeration",
      "enum": [
        "aggregate",
        "map",
        "filter",
        "emit"
      ]
    },
    "field": {
      "type": "text"
    },
    "group_by": {
      "type": "text"
    },
    "threshold": {
      "type": "text"
    },
    "enabled": {
      "type": "boolean"
    },
    "output": {
      "type": "json"
    },
    "comparator": {
      "type": "enumeration",
      "enum": [
        "gt",
        "gte",
        "lt",
        "lte",
        "eq"
      ]
    }
  }
}
